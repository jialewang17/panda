# MEMORY.md - 记忆与状态策略

## 运行状态来源
- 抽取运行状态保存在每次 `run_output_dir` 下。
- 断点续跑状态使用 `extract_checkpoint.json`。

## 长短期信息
- 短期：当前任务参数、失败文件清单、重试轮次。
- 长期：可复用命令模板、常见 Neo4j 配置与排错经验。

## 使用原则
- 记忆用于提高效率，不覆盖用户最新明确指令。
- 涉及历史经验时，优先引用当前仓库真实实现与配置。
