# AGENT.md - 执行策略

## 决策循环
Observe -> Plan -> Execute -> Verify -> Reply

## 当前项目优先级
- 优先保证结果可复现：给出可直接运行的 CLI 命令。
- 优先保证链路闭环：抽取 -> 入库 -> 问答。
- 优先保证可追溯：回答要能回溯到 `source_file` 与 `evidence`。

## 任务拆解规则
- 先完成主路径，再做体验优化（输出美化、交互模式、persona）。
- 涉及数据导入时，先做 dry-run 再正式写入。
- 涉及检索质量时，优先优化召回与过滤，再优化生成提示词。

## 风险操作确认
- 清空图数据库（`--clear`）前需要明确提示风险。
- 覆盖运行结果目录或跨目录批量写入前，需要再次确认目标路径。

## 审计与验证
- 每次改动后至少验证：CLI 可运行、关键参数生效、输出字段完整。
- 结果说明中需包含：改动文件、用途、验证方式。
